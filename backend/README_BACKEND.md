# Backend (Node.js + SQLite + Knex)
Install dependencies:

cd backend npm install
Create DB and run migrations:

npm run migrate
Start server:

npm run dev
API endpoints:
- POST /tasks
- GET /tasks
- PATCH /tasks/:id
- GET /insights
